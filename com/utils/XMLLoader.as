package com.utils{		import flash.display.*;	import flash.events.*;	import flash.net.*;	import flash.utils.*;		public class XMLLoader extends Sprite {				public function XMLLoader(xmlPath:String){			var loader:URLLoader = new URLLoader();			//loader.dataFormat = DataFormat.text;			loader.addEventListener( Event.COMPLETE, handleComplete);			loader.load( new URLRequest(xmlPath));					}		private function handleComplete (event:Event):void {			try{								var JKData:XML = new XML(event.target.data);				//trace(JKData);								//trace(xml.section.item);				var il:XMLList = JKData.section.entry.copy;				trace(il[0]);								//var aboutTxt:TextField = new TextField();				//aboutTxt.width = 300;				//aboutTxt.wordWrap = true;				//aboutTxt.text = il;								//addChild(aboutTxt);							} catch (e:TypeError){								trace("---ERROR - Could not parse XML---");				trace(e.message);							}					}			}				}