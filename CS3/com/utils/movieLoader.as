package com.utils {	import flash.display.*;	import flash.events.*;	import flash.net.URLRequest;		public class movieLoader extends MovieClip {		var mLoader:Loader;		var mainMC:DisplayObject;		public function movieLoader(main:DisplayObject, swfPath:String) {			mainMC = main;			mLoader=new Loader;			configureListeners(mLoader.contentLoaderInfo);			var request:URLRequest=new URLRequest(swfPath);			mLoader.load(request);		}				function completeHandler(event:Event):void {			mainMC.addChild(mLoader);			//if you want to move the swf around on the stage			//mLoader.x += 100;			//mLoader.y += 100;		}		private function configureListeners(dispatcher:IEventDispatcher):void {			dispatcher.addEventListener(Event.COMPLETE, completeHandler);		}	}}